# Aseprite MCP tool catalog

Generated by `python aseprite/tools/generate_catalog.py`. Do not edit manually.

Total tools: **85**.

## Profiles

| Profile | Categories | Tools |
| --- | --- | ---: |
| `core` | core | 3 |
| `sprite` | core, documents, export, palettes, pixels | 52 |
| `animation` | animation, core, documents, export, palettes, pixels, validation | 74 |
| `tiles` | core, documents, export, palettes, pixels, tiles, validation | 74 |
| `export` | core, export | 14 |
| `qa` | core, validation | 14 |
| `full` | animation, core, documents, export, palettes, pixels, tiles, validation | 85 |

## Animation

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_bake_tag_direction` | write | file | required | Append a forward frame sequence that materializes a tag's playback direction. |
| `aseprite_copy_cel` | write | file | required | Copy or link an existing image cel to another layer/frame location. |
| `aseprite_create_animation` | write | file | required | Create an animation with per-frame durations, layer cels, pixels, and tags. |
| `aseprite_edit_animation_events` | write | file | required | Set or remove structured frame events stored in sprite metadata. |
| `aseprite_edit_frames` | write | file | required | Add, duplicate, remove, or retime frames in sequential operation order. |
| `aseprite_edit_tags` | write | file | required | Create/update or remove animation tags in sequential operation order. |
| `aseprite_generate_inbetweens` | write | file | required | Insert bounded tween frames for one cel while preserving the starting frame's layers. |
| `aseprite_optimize_linked_cels` | write | file | required | Link exactly identical selected cel images without deleting animation frames. |
| `aseprite_preview_animation` | read | inline-image | required | Return an inline animated GIF for all frames or one tag. |
| `aseprite_preview_onion_skin` | read | inline-image | required | Return a PNG with earlier frames tinted red and later frames tinted blue. |
| `aseprite_retime_animation` | write | file | required | Retime all frames or one tag to an FPS, total duration, or scale factor. |

## Core

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_health` | read | structured | not required | Check the server configuration and installed Aseprite version without modifying files. |
| `aseprite_inspect_sprite` | read | structured | required | Inspect sprite dimensions, frames, layers, tags, slices, and palettes. |
| `aseprite_list_capabilities` | read | structured | not required | List supported tools, profiles, mutability, output kinds, and availability. |

## Documents

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_convert_color_mode` | write | file | required | Convert a sprite color mode with an explicit bounded dithering choice. |
| `aseprite_copy_layer_tree` | write | file | required | Copy one image-layer or group hierarchy from a compatible donor document. |
| `aseprite_create_sprite` | write | file | required | Create a bounded Aseprite document with optional initial pixels. |
| `aseprite_crop_sprite` | write | file | required | Crop the canvas to visible pixels in selected frames and layers, with padding. |
| `aseprite_edit_blend_modes` | write | file | required | Apply validated blend modes to exact non-group layer paths. |
| `aseprite_edit_cels` | write | file | required | Move, fade, reorder, unlink, or remove exact layer/frame cels. |
| `aseprite_edit_color_space` | write | file | required | Assign or convert a document color space using sRGB or an authorized ICC profile. |
| `aseprite_edit_grid` | write | file | required | Set sprite grid origin and cell dimensions in a new document revision. |
| `aseprite_edit_layers` | write | file | required | Add, remove, rename, show, hide, reorder, or regroup layers sequentially. |
| `aseprite_edit_properties` | write | file | required | Set or remove scalar user properties on sprite production objects. |
| `aseprite_edit_slices` | write | file | required | Create/update frame-specific slices or remove named slices. |
| `aseprite_import_frames` | write | file | required | Append or insert explicit same-size PNG files as frames on one image layer. |
| `aseprite_import_sprite_sheet` | write | file | required | Import a PNG grid as an editable Aseprite animation with one cel per frame. |
| `aseprite_inspect_properties` | read | structured | required | Inspect scalar properties, user data, and timeline colors across document objects. |
| `aseprite_merge_layers` | write | file | required | Merge one layer downward or flatten all/visible layers in an output revision. |
| `aseprite_resize_canvas` | write | file | required | Resize the transparent canvas without scaling sprite artwork. |
| `aseprite_resize_sprite` | write | file | required | Scale an Aseprite document and its cels using nearest-neighbor pixel scaling. |
| `aseprite_trim_cels` | write | file | required | Trim transparent cel borders while preserving canvas placement. |

## Export

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_batch_export` | write | file | required | Export bounded sprite-sheet jobs independently and report per-job failures. |
| `aseprite_export_bitmap_font` | write | file | required | Export explicit glyph rectangles as a PNG atlas and deterministic JSON metrics. |
| `aseprite_export_frames` | write | file | required | Export explicit zero-based frames to explicit PNG output paths. |
| `aseprite_export_sprite_sheet` | write | file | required | Export a PNG sprite sheet and JSON-array frame metadata. |
| `aseprite_extract_slices` | write | file | required | Render explicitly named slice/frame pairs to explicit PNG output paths. |
| `aseprite_pack_atlas` | write | file | required | Pack up to 64 authorized sprite files into a PNG atlas and JSON metadata. |
| `aseprite_preview` | read | inline-image | required | Return an inline PNG preview of one frame or a sprite sheet. |
| `aseprite_preview_nine_slice` | read | inline-image | required | Return an inline PNG showing a named nine-slice at a requested size. |
| `aseprite_render` | write | file | required | Render one frame to PNG or a frame/tag selection to GIF. |
| `aseprite_render_contact_sheet` | write | file | required | Render every frame into a labelled PNG review grid. |
| `aseprite_render_layer_variants` | write | file | required | Render named layer combinations to explicit PNG or GIF outputs. |

## Palettes

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_analyze_palette` | read | structured | required | Analyze exact color usage, unused entries, and nearby colors. |
| `aseprite_apply_palette` | write | file | required | Remap cel colors to the nearest supplied palette colors. |
| `aseprite_edit_palette_entries` | write | file | required | Set, append, remove, or swap indexed palette entries safely. |
| `aseprite_export_palette` | write | file | required | Export the first sprite palette to an authorized palette file. |
| `aseprite_import_palette` | write | file | required | Load a supported palette file into a new sprite-document revision. |
| `aseprite_palette_cycle` | write | file | required | Rotate selected indexed-color entries progressively across an existing frame range. |
| `aseprite_quantize_palette` | write | file | required | Create a bounded palette and convert a document to indexed color. |
| `aseprite_replace_color` | write | file | required | Replace a color across selected image layers and frames. |

## Pixels

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_apply_outline` | write | file | required | Apply a fixed inside or outside outline to one cel. |
| `aseprite_draw_shapes` | write | file | required | Draw fixed lines, rectangles, and ellipses on one layer/frame. |
| `aseprite_draw_strokes` | write | file | required | Draw bounded pencil strokes with explicit points, brush size, and opacity. |
| `aseprite_edit_selection` | write | file | required | Replace, combine, clear, or select all of the document selection mask. |
| `aseprite_fill_region` | write | file | required | Flood-fill a bounded layer/frame region from one coordinate. |
| `aseprite_read_composited_pixels` | read | structured | required | Read final visible frame pixels as compact row-based RGBA runs. |
| `aseprite_read_pixels` | read | structured | required | Read a bounded rectangle as compact row-based #RRGGBBAA runs. |
| `aseprite_select_by_color` | write | file | required | Build a document selection from matching layer or composited frame colors. |
| `aseprite_set_pixel_runs` | write | file | required | Write horizontal RGBA pixel runs efficiently to one layer and frame. |
| `aseprite_set_pixels` | write | file | required | Set bounded RGBA pixels on one image layer and frame, saving to output_path. |
| `aseprite_transform_cel` | write | file | required | Translate, flip, or quarter-turn one image cel. |
| `aseprite_transform_selection` | write | file | required | Move, copy, flip, quarter-turn, or nearest-neighbor scale a rectangular pixel region. |

## Tiles

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_create_tileset_from_sheet` | write | file | required | Slice one image-layer cel into a named tileset with optional deduplication. |
| `aseprite_edit_tile_metadata` | write | file | required | Set or remove scalar properties on an exact tileset or tile index. |
| `aseprite_edit_tilemap` | write | file | required | Create or edit a tilemap layer using bounded tile-cell records. |
| `aseprite_edit_tileset` | write | file | required | Create or rename tilesets and add, remove, or repaint their tiles. |
| `aseprite_export_tilemap_data` | write | file | required | Export selected tilemap layers and frames as deterministic versioned JSON. |
| `aseprite_export_tileset` | write | file | required | Export one tileset to a PNG grid and JSON metadata. |
| `aseprite_import_tilemap_data` | write | file | required | Apply validated versioned tilemap JSON to existing or new top-level tilemap layers. |
| `aseprite_inspect_tile_metadata` | read | structured | required | Read scalar properties and user metadata from a tileset and selected tiles. |
| `aseprite_inspect_tilesets` | read | structured | required | Inspect tileset dimensions/counts and tilemap layer paths. |
| `aseprite_render_tilemap_preview` | read | inline-image | required | Return an inline PNG for an explicit arrangement of tileset cells. |
| `aseprite_validate_tileset` | read | structured | required | Detect empty, duplicate, and non-seamless tiles. |

## Validation

| Tool | Access | Output | Aseprite | Description |
| --- | --- | --- | --- | --- |
| `aseprite_compare_frames` | write | file | required | Compare composited frames and optionally write a magenta difference image. |
| `aseprite_compare_sprites` | read | structured | required | Compare two sprites' structure, metadata, and composited frame pixels. |
| `aseprite_generate_collision_masks` | read | structured | required | Derive per-frame collision rectangles from composited or layer alpha. |
| `aseprite_generate_collision_polygons` | read | structured | required | Trace bounded outer collision contours from composited or layer alpha. |
| `aseprite_generate_motion_report` | read | structured | required | Measure bounds, centroid, velocity, acceleration, travel, and peak speed. |
| `aseprite_inspect_cels` | read | structured | required | Inspect cel geometry, opacity, z-index, image identity, and linked groups. |
| `aseprite_validate_animation` | read | structured | required | Validate frame occupancy, bounds, baseline consistency, sizing, and duplicates. |
| `aseprite_validate_asset_set` | read | structured | required | Validate multiple assets against one profile and cross-asset consistency rules. |
| `aseprite_validate_export_profile` | read | structured | required | Validate a sprite against explicit engine-facing dimensions and naming requirements. |
| `aseprite_validate_loop_transition` | read | structured | required | Validate visual and optional timing continuity between loop endpoints. |
| `aseprite_validate_pixel_art` | read | structured | required | Check color count, alpha, palette compliance, and isolated opaque pixels. |
